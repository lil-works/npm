{% extends 'base.html.twig' %}

{% block body %}
    <h1>Descriptor creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('manager_interfero') }}">Back to the current interfero</a>
        </li>
    </ul>
{% endblock %}
