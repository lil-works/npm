{% extends 'base.html.twig' %}

{% block body %}
    <h1>Synonym</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ synonym.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ synonym.label }}</td>
            </tr>


        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('manager_synonym') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('manager_synonym_edit', { 'id': synonym.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
